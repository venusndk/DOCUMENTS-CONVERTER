# Only needed to run the HTTP API (documents_converter/api/). The CLI
# (scan_to_excel.py) does not need these -- install alongside
# requirements.txt: pip install -r requirements.txt -r requirements-api.txt
fastapi==0.141.1
uvicorn==0.52.1
python-multipart==0.0.32

# Phase 1 completion (master directive numbering): persistence layer for
# the job store (documents_converter/api/jobs.py, db.py, models.py) and
# its Alembic migrations (alembic.ini, migrations/). psycopg is the
# Postgres driver -- needed even if you only ever run against the
# default local SQLite (stdlib sqlite3, no extra package) so that
# DATABASE_URL can point at Postgres without a second install step.
sqlalchemy==2.0.52
alembic==1.19.2
psycopg[binary]==3.3.5

# Phase 14 (master directive numbering): Redis-backed job queue
# (documents_converter/api/job_queue.py, rq_tasks.py, worker_main.py)
# replacing the in-process ThreadPoolExecutor for /api/v1/jobs and
# /api/v1/batch. Needed by both the api and worker processes/containers
# (docker-compose.yml) -- the worker imports the same documents_converter
# package to actually run a conversion, not just to talk to Redis.
redis==8.1.0
rq==2.12.0

# Phase 16 (master directive numbering): AI Document Intelligence
# (documents_converter/ai_intelligence.py) -- vision fallback,
# structured extraction, summarization, translation, intelligent
# classification. Google Gemini, not Anthropic -- see that module's
# own docstring and config.py's GEMINI_API_KEY for why. Entirely
# optional at runtime (config.is_ai_available() gates every endpoint
# that uses it), but the package itself is a normal, always-installed
# API dependency, same as redis/rq above.
google-genai==2.23.0
